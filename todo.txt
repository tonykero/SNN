Layer:
    -instances count (static member) (to define id)
    -feed()

FFNet:
    -constructor
    -feed()
    -randWeights()
    -link()

Neuron:
    -instances count (to define id)
    -compute()
    -store()
